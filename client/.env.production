# In production the Express server serves the built client from the same origin,
# so API requests should use relative URLs (same-origin). Leaving this empty makes
# apiClient's baseURL fall back to '' → requests go to the current host.
VITE_API_URL=
